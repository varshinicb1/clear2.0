// Auto-generated by Clear v0.2 — Fastify
// Source: HealthPlatform
// Do not edit — changes should be made to the .clear file

import Fastify from 'fastify'
import cors from '@fastify/cors'
import { v4 as uuidv4 } from 'uuid'

// Data: Patient
export interface Patient {
  id?: string
  name: string
  email: string
  phone?: string
  date_of_birth?: string
  blood_type?: 'A+' | 'A-' | 'B+' | 'B-' | 'AB+' | 'AB-' | 'O+' | 'O-'
  allergies?: string[]
  created_at?: string
}

// Data: Doctor
export interface Doctor {
  id?: string
  name: string
  email: string
  specialty: string
  license_num?: string
}

// Data: Appointment
export interface Appointment {
  id?: string
  patient: Patient
  doctor: Doctor
  date_time: string
  duration_min?: number
  status?: 'scheduled' | 'confirmed' | 'in_progress' | 'completed' | 'cancelled' | 'no_show'
  reason?: string
  notes?: string
  created_at?: string
}

// Data: MedicalRecord
export interface MedicalRecord {
  id?: string
  patient: Patient
  doctor: Doctor
  diagnosis: string
  prescriptions?: Prescription[]
  notes?: string
  attachments?: url[]
  created_at?: string
}

// Data: Prescription
export interface Prescription {
  id?: string
  medication: string
  dosage: string
  frequency: string
  duration_days?: number
  refills?: number
  notes?: string
}

// In-memory stores
const patients: Patient[] = []
const doctors: Doctor[] = []
const appointments: Appointment[] = []
const medicalrecords: MedicalRecord[] = []
const prescriptions: Prescription[] = []

// Rule: AppointmentValidation
function validateAppointmentValidation(data: Record<string, any>): string[] {
  const errors: string[] = []
  if (data['date_time'] && new Date(data['date_time']).getTime() <= Date.now()) errors.push('date_time must be in the future')
  // TODO: validate patient exists in Patient
  // TODO: validate doctor exists in Doctor
  return errors
}

// App setup
const fastify = Fastify()

fastify.register(cors, { origin: "*" })

// API: REST /api/patients
fastify.get('/api/patients', async (request, reply) => {
  try {
    let result = [...patients]
    result.sort((a: any, b: any) => {
      const av = a['name']
      const bv = b['name']
      if (!av && !bv) return 0
      if (!av) return 1
      if (!bv) return -1
      return av < bv ? -1 : av > bv ? 1 : 0
    })
    const page = parseInt((request.query as any).page || '1')
    const start = (page - 1) * 20
    return {
      data: result.slice(start, start + 20),
      total: result.length,
      page,
      limit: 20,
      totalPages: Math.ceil(result.length / 20)
    }
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})
fastify.get('/api/patients/:id', async (request, reply) => {
  try {
    const id = (request.params as any).id
    const item = patients.find(i => i.id === id)
    if (!item) {
      reply.code(404)
      return { error: 'not found' }
    }
    return item
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})
fastify.post('/api/patients', async (request, reply) => {
  try {
    const { name, email, phone, date_of_birth, blood_type, allergies } = request.body as any
    const item: Patient = {
      id: uuidv4(),
      name,
      email,
      phone,
      date_of_birth,
      blood_type,
      allergies,
      created_at: new Date().toISOString(),
    }
    patients.push(item)
    reply.code(201)
    return item
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})
fastify.put('/api/patients/:id', async (request, reply) => {
  try {
    const id = (request.params as any).id
    const index = patients.findIndex(i => i.id === id)
    if (index === -1) {
      reply.code(404)
      return { error: 'not found' }
    }
    const updates: Partial<Patient> = {}
    if ((request.body as any)['name'] !== undefined) updates['name'] = (request.body as any)['name']
    if ((request.body as any)['phone'] !== undefined) updates['phone'] = (request.body as any)['phone']
    if ((request.body as any)['allergies'] !== undefined) updates['allergies'] = (request.body as any)['allergies']
    if ((request.body as any)['blood_type'] !== undefined) updates['blood_type'] = (request.body as any)['blood_type']
    patients[index] = { ...patients[index], ...updates, id }
    reply.code(200)
    return patients[index]
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})

// API: REST /api/appointments
fastify.get('/api/appointments', async (request, reply) => {
  try {
    let result = [...appointments]
    if ((request.query as any)['status']) result = result.filter(i => String(i['status']) === (request.query as any)['status'])
    if ((request.query as any)['patient']) result = result.filter(i => String(i['patient']) === (request.query as any)['patient'])
    if ((request.query as any)['doctor']) result = result.filter(i => String(i['doctor']) === (request.query as any)['doctor'])
    result.sort((a: any, b: any) => {
      const av = a['date_time']
      const bv = b['date_time']
      if (!av && !bv) return 0
      if (!av) return 1
      if (!bv) return -1
      return av < bv ? -1 : av > bv ? 1 : 0
    })
    const page = parseInt((request.query as any).page || '1')
    const start = (page - 1) * 30
    return {
      data: result.slice(start, start + 30),
      total: result.length,
      page,
      limit: 30,
      totalPages: Math.ceil(result.length / 30)
    }
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})
fastify.get('/api/appointments/:id', async (request, reply) => {
  try {
    const id = (request.params as any).id
    const item = appointments.find(i => i.id === id)
    if (!item) {
      reply.code(404)
      return { error: 'not found' }
    }
    const result = { ...item }
    result['patient'] = patients.find((r: any) => r.id === item['patient']) ?? null
    return result
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})
fastify.post('/api/appointments', async (request, reply) => {
  try {
    const { patient, doctor, date_time, duration_min, reason } = request.body as any
    const validationErrors: string[] = []
    validationErrors.push(...validateAppointmentValidation(request.body as any))
    if (validationErrors.length > 0) {
      reply.code(400)
      return { errors: validationErrors }
    }
    const item: Appointment = {
      id: uuidv4(),
      patient,
      doctor,
      date_time,
      duration_min,
      reason,
      created_at: new Date().toISOString(),
    }
    appointments.push(item)
    reply.code(201)
    return item
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})
fastify.put('/api/appointments/:id/status', async (request, reply) => {
  try {
    const id = (request.params as any).id
    const index = appointments.findIndex(i => i.id === id)
    if (index === -1) {
      reply.code(404)
      return { error: 'not found' }
    }
    const updates: Partial<Appointment> = {}
    if ((request.body as any)['status'] !== undefined) updates['status'] = (request.body as any)['status']
    appointments[index] = { ...appointments[index], ...updates, id }
    reply.code(200)
    return appointments[index]
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})

// API: REST /api/records
fastify.get('/api/records', async (request, reply) => {
  try {
    let result = [...medicalrecords]
    result.sort((a: any, b: any) => {
      const av = a['created_at']
      const bv = b['created_at']
      if (!av && !bv) return 0
      if (!av) return 1
      if (!bv) return -1
      return av < bv ? 1 : av > bv ? -1 : 0
    })
    const page = parseInt((request.query as any).page || '1')
    const start = (page - 1) * 20
    return {
      data: result.slice(start, start + 20),
      total: result.length,
      page,
      limit: 20,
      totalPages: Math.ceil(result.length / 20)
    }
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})
fastify.get('/api/records/:id', async (request, reply) => {
  try {
    const id = (request.params as any).id
    const item = medicalrecords.find(i => i.id === id)
    if (!item) {
      reply.code(404)
      return { error: 'not found' }
    }
    const result = { ...item }
    result['patient'] = patients.find((r: any) => r.id === item['patient']) ?? null
    return result
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})
fastify.post('/api/records', async (request, reply) => {
  try {
    const { patient, doctor, diagnosis, prescriptions, notes } = request.body as any
    const item: MedicalRecord = {
      id: uuidv4(),
      patient,
      doctor,
      diagnosis,
      prescriptions,
      notes,
      created_at: new Date().toISOString(),
    }
    medicalrecords.push(item)
    reply.code(201)
    return item
  } catch (err: any) {
    console.error(err)
    reply.code(500).send({ error: err.message })
  }
})

// Error handling
fastify.setErrorHandler((error, _request, reply) => {
  console.error("Error:", error.message)
  reply.code(error.statusCode || 500).send({ error: error.message })
})

const PORT = process.env.PORT ?? 8080
fastify.listen({ port: Number(PORT) }, (err, address) => {
  if (err) {
    console.error(err)
    process.exit(1)
  }
  console.log(`Server running on port ${PORT}`)
})

export default fastify