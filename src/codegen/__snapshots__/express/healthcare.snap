// Auto-generated by Clear v0.2 — Express.js
// Source: HealthPlatform
// Do not edit — changes should be made to the .clear file

import express, { Request, Response, NextFunction } from 'express'
import cors from 'cors'
import { v4 as uuidv4 } from 'uuid'

// Data: Patient
export interface Patient {
  id?: string
  name: string
  email: string
  phone?: string
  date_of_birth?: string
  blood_type?: 'A+' | 'A-' | 'B+' | 'B-' | 'AB+' | 'AB-' | 'O+' | 'O-'
  allergies?: string[]
  created_at?: string
}

// Data: Doctor
export interface Doctor {
  id?: string
  name: string
  email: string
  specialty: string
  license_num?: string
}

// Data: Appointment
export interface Appointment {
  id?: string
  patient: Patient
  doctor: Doctor
  date_time: string
  duration_min?: number
  status?: 'scheduled' | 'confirmed' | 'in_progress' | 'completed' | 'cancelled' | 'no_show'
  reason?: string
  notes?: string
  created_at?: string
}

// Data: MedicalRecord
export interface MedicalRecord {
  id?: string
  patient: Patient
  doctor: Doctor
  diagnosis: string
  prescriptions?: Prescription[]
  notes?: string
  attachments?: url[]
  created_at?: string
}

// Data: Prescription
export interface Prescription {
  id?: string
  medication: string
  dosage: string
  frequency: string
  duration_days?: number
  refills?: number
  notes?: string
}

// In-memory stores
const patients: Patient[] = []
const doctors: Doctor[] = []
const appointments: Appointment[] = []
const medicalrecords: MedicalRecord[] = []
const prescriptions: Prescription[] = []

// Rule: AppointmentValidation
function validateAppointmentValidation(data: Record<string, any>): string[] {
  const errors: string[] = []
  if (data['date_time'] && new Date(data['date_time']).getTime() <= Date.now()) errors.push('date_time must be in the future')
  // TODO: validate patient exists in Patient
  // TODO: validate doctor exists in Doctor
  return errors
}

// App setup
const app = express()
app.use(cors())
app.use(express.json())

// API: REST /api/patients
app.get('/api/patients', (req: Request, res: Response) => {
  try {
    let result = [...patients]
    result.sort((a: any, b: any) => {
      const av = a['name']
      const bv = b['name']
      if (!av && !bv) return 0
      if (!av) return 1
      if (!bv) return -1
      return av < bv ? -1 : av > bv ? 1 : 0
    })
    const page = parseInt(req.query.page as string) || 1
    const start = (page - 1) * 20
    res.json({
      data: result.slice(start, start + 20),
      total: result.length,
      page,
      limit: 20,
      totalPages: Math.ceil(result.length / 20)
    })
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})
app.get('/api/patients/:id', (req: Request, res: Response) => {
  try {
    const item = patients.find(i => i.id === req.params.id)
    if (!item) {
      res.status(404).json({ error: 'not found' })
      return
    }
    res.json(item)
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})
app.post('/api/patients', (req: Request, res: Response) => {
  try {
    const { name, email, phone, date_of_birth, blood_type, allergies } = req.body
    const item: Patient = {
      id: uuidv4(),
      name,
      email,
      phone,
      date_of_birth,
      blood_type,
      allergies,
      created_at: new Date().toISOString(),
    }
    patients.push(item)
    res.status(201).json(item)
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})
app.put('/api/patients/:id', (req: Request, res: Response) => {
  try {
    const index = patients.findIndex(i => i.id === req.params.id)
    if (index === -1) {
      res.status(404).json({ error: 'not found' })
      return
    }
    const updates: Partial<Patient> = {}
    if (req.body['name'] !== undefined) updates['name'] = req.body['name']
    if (req.body['phone'] !== undefined) updates['phone'] = req.body['phone']
    if (req.body['allergies'] !== undefined) updates['allergies'] = req.body['allergies']
    if (req.body['blood_type'] !== undefined) updates['blood_type'] = req.body['blood_type']
    patients[index] = { ...patients[index], ...updates, id: req.params.id }
    res.status(200).json(patients[index])
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})

// API: REST /api/appointments
app.get('/api/appointments', (req: Request, res: Response) => {
  try {
    let result = [...appointments]
    if (req.query['status']) result = result.filter(i => String(i['status']) === req.query['status'])
    if (req.query['patient']) result = result.filter(i => String(i['patient']) === req.query['patient'])
    if (req.query['doctor']) result = result.filter(i => String(i['doctor']) === req.query['doctor'])
    result.sort((a: any, b: any) => {
      const av = a['date_time']
      const bv = b['date_time']
      if (!av && !bv) return 0
      if (!av) return 1
      if (!bv) return -1
      return av < bv ? -1 : av > bv ? 1 : 0
    })
    const page = parseInt(req.query.page as string) || 1
    const start = (page - 1) * 30
    res.json({
      data: result.slice(start, start + 30),
      total: result.length,
      page,
      limit: 30,
      totalPages: Math.ceil(result.length / 30)
    })
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})
app.get('/api/appointments/:id', (req: Request, res: Response) => {
  try {
    const item = appointments.find(i => i.id === req.params.id)
    if (!item) {
      res.status(404).json({ error: 'not found' })
      return
    }
    const result = { ...item }
    result['patient'] = patients.find((r: any) => r.id === item['patient']) ?? null
    res.json(result)
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})
app.post('/api/appointments', (req: Request, res: Response) => {
  try {
    const { patient, doctor, date_time, duration_min, reason } = req.body
    const validationErrors: string[] = []
    validationErrors.push(...validateAppointmentValidation(req.body))
    if (validationErrors.length > 0) {
      res.status(400).json({ errors: validationErrors })
      return
    }
    const item: Appointment = {
      id: uuidv4(),
      patient,
      doctor,
      date_time,
      duration_min,
      reason,
      created_at: new Date().toISOString(),
    }
    appointments.push(item)
    res.status(201).json(item)
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})
app.put('/api/appointments/:id/status', (req: Request, res: Response) => {
  try {
    const index = appointments.findIndex(i => i.id === req.params.id)
    if (index === -1) {
      res.status(404).json({ error: 'not found' })
      return
    }
    const updates: Partial<Appointment> = {}
    if (req.body['status'] !== undefined) updates['status'] = req.body['status']
    appointments[index] = { ...appointments[index], ...updates, id: req.params.id }
    res.status(200).json(appointments[index])
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})

// API: REST /api/records
app.get('/api/records', (req: Request, res: Response) => {
  try {
    let result = [...medicalrecords]
    result.sort((a: any, b: any) => {
      const av = a['created_at']
      const bv = b['created_at']
      if (!av && !bv) return 0
      if (!av) return 1
      if (!bv) return -1
      return av < bv ? 1 : av > bv ? -1 : 0
    })
    const page = parseInt(req.query.page as string) || 1
    const start = (page - 1) * 20
    res.json({
      data: result.slice(start, start + 20),
      total: result.length,
      page,
      limit: 20,
      totalPages: Math.ceil(result.length / 20)
    })
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})
app.get('/api/records/:id', (req: Request, res: Response) => {
  try {
    const item = medicalrecords.find(i => i.id === req.params.id)
    if (!item) {
      res.status(404).json({ error: 'not found' })
      return
    }
    const result = { ...item }
    result['patient'] = patients.find((r: any) => r.id === item['patient']) ?? null
    res.json(result)
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})
app.post('/api/records', (req: Request, res: Response) => {
  try {
    const { patient, doctor, diagnosis, prescriptions, notes } = req.body
    const item: MedicalRecord = {
      id: uuidv4(),
      patient,
      doctor,
      diagnosis,
      prescriptions,
      notes,
      created_at: new Date().toISOString(),
    }
    medicalrecords.push(item)
    res.status(201).json(item)
  } catch (err: any) {
    console.error(err)
    res.status(500).json({ error: err.message })
  }
})

// Error handling
app.use((err: Error, _req: Request, res: Response, _next: NextFunction) => {
  console.error("Error:", err.message)
  res.status(500).json({ error: err.message })
})

const PORT = process.env.PORT ?? 8080
app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`)
})

export default app